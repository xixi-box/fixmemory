interface:
  display_name: "FixMemory"
  short_description: "Reuse verified debugging fixes across coding agents"
  default_prompt: "Use $fixmemory to search prior verified fixes before debugging this failure."
policy:
  allow_implicit_invocation: true
